# Local development only. GitHub Actions reads these from repository secrets
# instead, so this file never needs to be committed.
#
# Copy to .env, fill in from Supabase → Project Settings → API.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
